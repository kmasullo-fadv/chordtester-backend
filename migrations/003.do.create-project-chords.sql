CREATE TABLE project_chords (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    project_id INTEGER REFERENCES user_projects(id) ON DELETE CASCADE NOT NULL,
    notes TEXT NOT NULL
)