CREATE TABLE user_projects (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    username TEXT REFERENCES chordtester_users(username) ON DELETE CASCADE NOT NULL,
    title TEXT NOT NULL
)